- title "Edit Album"

= render :partial => 'form'

%p
  = link_to "Show", album_path(@album)
  |
  -#= link_to "View All", albums_path
  -#|
  = link_to t('Albums') + " " + sprintf(t("for album %s"), h(@album.category.title)), category_albums_path(@album.category)
